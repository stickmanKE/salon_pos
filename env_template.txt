# ============================================================
# salon_system/.env
# Store this file LOCALLY only — never commit it to GitHub
# ============================================================

SECRET_KEY=replace-this-with-a-long-random-string-50-chars
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=salon_db
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432
